apiVersion: v1
kind: Service
metadata:
  name: tldraw-sync
  namespace: tldraw
spec:
  selector:
    app: tldraw-sync
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
